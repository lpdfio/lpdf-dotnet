namespace Lpdf.Canvas;

/// <summary>Line join style for stroked paths.</summary>
public enum LineJoin
{
    /// <summary>Pointed join (default).</summary>
    Miter,
    /// <summary>Rounded join.</summary>
    Round,
    /// <summary>Bevelled join.</summary>
    Bevel,
}
